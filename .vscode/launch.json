{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Sudoku (macOS)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/sudoku",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "preLaunchTask": "build sudoku",
            "MIMode": "lldb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for lldb",
                    "text": "command script import lldb.macos.prettyprint",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}