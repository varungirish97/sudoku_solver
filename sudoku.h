#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <vector>

void printPuzzle(std::vector<std::vector<int>> puzzle);


/*

Define two classes, one for each grid and one for the entire sudoku.
Add elements like row, column, number, code etc to the grid.


*/